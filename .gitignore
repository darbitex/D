# Secrets — never commit
.env
.env.*
*.env
!.env.example
!**/.env.example
.sui_keystore
sui_config/
*.key
*-keys/
**/.aptos/config.yaml

# Build / dependency artifacts
node_modules/
build/
**/build/

# Sui Move auto-regenerated
**/Move.lock

# Language runtime
__pycache__/
*.pyc

# OS
.DS_Store
Thumbs.db

# Editor
*.swp
*.swo

# Deploy artifacts intermediate (Published.toml + publish-output.json kept; raw stripped)
**/deploy-scripts/publish-raw.json

# Generated test wallets — contain ed25519 private keys
**/deploy-scripts/wallets.json
**/wallets.json

# Compiled Move bytecode (regenerable via `aptos move compile-script`)
**/scripts_bytecode/

# Backup deps with mainnet/testnet suffix (only for active toml)
**/Move.toml.mainnet
**/Move.toml.testnet
