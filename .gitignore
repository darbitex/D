# Secrets — never commit
.env
*.env
.sui_keystore
sui_config/
*.key
*-keys/

# Build / dependency artifacts
node_modules/
build/
**/build/

# Sui Move auto-regenerated
**/Move.lock

# Language runtime
__pycache__/
*.pyc

# OS
.DS_Store
Thumbs.db

# Editor
*.swp
*.swo

# Deploy artifacts intermediate (Published.toml + publish-output.json kept; raw stripped)
**/deploy-scripts/publish-raw.json

# Backup deps with mainnet/testnet suffix (only for active toml)
**/Move.toml.mainnet
**/Move.toml.testnet
