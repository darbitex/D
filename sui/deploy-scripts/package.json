{
  "name": "d-sui-deploy-scripts",
  "version": "0.1.0",
  "private": true,
  "description": "D Sui deploy scripts — Tx 2 seal PTB (finalize_registration + destroy_cap)",
  "scripts": {
    "seal": "ts-node seal.ts",
    "verify": "ts-node verify.ts"
  },
  "dependencies": {
    "@mysten/sui": "^1.20.0",
    "@pythnetwork/pyth-sui-js": "^2.4.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3"
  }
}
